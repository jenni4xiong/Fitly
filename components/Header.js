import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Header = (props) => {
  return (
    <View>
      <Text>
        hello header
      </Text>
    </View>
  )
}

export default Header;